# Legacy & internal material

This folder is **gitignored** (except this file). Use it for:

- Old internal test artifacts (e.g. `speed-test*.txt` that lived under `src/assets/`)
- Deprecated notes or one-off documentation you do **not** want in the main repo

Remove or add files here locally; they will not be committed if listed under `old-doc/*` in `.gitignore`.
